{
  "name": "doudochain_amoy",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ doudochain_amoy",
    "create-local": "graph create --node http://localhost:8020/ doudochain_amoy",
    "remove-local": "graph remove --node http://localhost:8020/ doudochain_amoy",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 doudochain_amoy",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.90.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
